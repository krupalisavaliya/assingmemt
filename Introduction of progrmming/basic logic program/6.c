//6. Find area of Triangle Formula : A = 1/2 × b × h
#include<stdio.h>
int main()
{
	float area, b=4 , h=5;
	area = (1.0 / 2) *b*h;
	printf("\n Area of triangle formula: A=1/2*b*h  : %.2f",area);
}
